import { describe, it } from "vitest";

describe("parseAddressComponents", () => {
  it.todo("parses a complete US address (street + city + state + zip)");
  it.todo("returns an empty zip when postal_code is missing");
});
